{{!< default}}
	{{> "components/homepage/horizontal-scroll"}}
	<div class="split-content-wrapper">
	{{! Main Content Area (2/3 width) }}
	<div class="main-content-area">
		{{> "components/homepage/cta-banner"}}
		{{> "components/homepage/masonry-grid"}}
		{{> "components/homepage/post-feed"}}
	</div>
	<aside class="content-aside">
		{{> "components/homepage/aside-content"}}
	</aside>
</div>
{{! Search functionality }}
<script>
document.addEventListener('DOMContentLoaded', function() {
	const searchInput = document.querySelector('.search-input');
	if (searchInput) {
		searchInput.addEventListener('keypress', function(e) {
			if (e.key === 'Enter') {
				const searchTerm = this.value.trim();
				if (searchTerm) {
					window.location.href = `{{@site.url}}/search/?q=${encodeURIComponent(searchTerm)}`;
				}
			}
		});
	}
});
</script>